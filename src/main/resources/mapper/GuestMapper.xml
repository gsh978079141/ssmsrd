<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsh.ssmsrd.dao.GuestMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.gsh.ssmsrd.model.Guest">
        <id column="id" property="id" />
        <result column="roomid" property="roomid" />
        <result column="guestname" property="guestname" />
        <result column="tel" property="tel" />
        <result column="sex" property="sex" />
        <result column="idtype" property="idtype" />
        <result column="idnum" property="idnum" />
        <result column="area" property="area" />
        <result column="guesttype" property="guesttype" />
        <result column="guestsource" property="guestsource" />
        <result column="corporatename" property="corporatename" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, roomid, guestname, tel, sex, idtype, idnum, area, guesttype, guestsource, corporatename, remark
    </sql>

</mapper>
